{
  "name": "lime-template-ssr-frontend",
  "description": "frontend",
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:client": "cross-env NODE_ENV=production BUILD_TARGET=client node build/build.js",
    "build:server": "cross-env NODE_ENV=production BUILD_TARGET=server node build/build.js"
  },
  "dependencies": {
    "vue": "^2.6.10",
    "vue-meta": "^1.6.0",
    "vue-router": "^3.0.2",
    "vuex": "^3.1.0",
    "vuex-router-sync": "^5.0.0"
  },
  "devDependencies": {}
}
